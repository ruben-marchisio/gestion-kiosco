document.addEventListener('DOMContentLoaded', () => {
  console.log('index-logic.js cargado');
  // No se requiere lógica adicional, la página solo muestra contenido estático
});
  /* Propósito: Inicializa el script cuando la página index.html está completamente cargada */
  /* Registra un evento 'DOMContentLoaded' para ejecutar el código una vez que el DOM esté listo */
  /* Imprime un mensaje en la consola para confirmar que el script se cargó */
  /* No incluye lógica adicional, ya que index.html es una página estática con enlaces a otras secciones */